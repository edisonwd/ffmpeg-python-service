# ffmpeg-python-service
ffmpeg-python-service

# 启动步骤

## 1. 安装依赖
执行命令 uv venv 来创建一个虚拟环境，会创建一个名为 .venv 的虚拟环境。
```shell
uv venv
```
激活虚拟环境
```shell
source .venv/bin/activate
```
安装依赖
```shell
uv pip install -r requirement.txt
```

## 2. 启动应用
```shell
python ffmpeg_tools.py
```


